#!/usr/bin/env bash
# bin/release <build-dir>

cat <<EOF
---
addons:
  - scheduler:standard
config_vars:
  PATH: /app/bin:/usr/local/bin:/usr/bin:/bin
  GEM_HOME: /app/vendor/.gem
default_process_types:
  web: bash web.sh
EOF
